Nivedha S
id: 21001

1 .	select ename 
	from emp
	where job='clerk';

2.	select ename
	from emp 
	where job='clerk' OR JOB= 'MANAGER';

3.	SELECT GETDATE();

4.	SELECT ENAME,EMPNO,SAL 
	FROM EMP 
	WHERE SAL BETWEEN 500 AND 1000;

5.	SELECT ENAME,EMPNO 
	FROM EMP 
	WHERE HIREDATE BETWEEN '1-DEC-81' AND '31-DEC-81'

6.	SELECT EMPNO,ENAME,SAL
	FROM EMP 
	WHERE SAL NOT BETWEEN 1000 AND 2500;

7.	SELECT EMPNO,ENAME,SAL
	FROM EMP 
	WHERE SAL IN(800 , 950 , 3000 , 5000);

8.	SELECT ENAME 
	FROM EMP 
	WHERE ENAME LIKE '_L%';

9.	SELECT ENAME 
	FROM EMP 
	WHERE ENAME LIKE '%D';

10.	SELECT ENAME 
	FROM EMP 
	WHERE ENAME LIKE '_____';

11.	SELECT ENAME 
	FROM EMP 
	WHERE ENAME LIKE 'T%R';

12.	SELECT ENAME 
	FROM EMP 
	WHERE ENAME LIKE '%A%';

13.	SELECT ENAME,HIREDATE 
	FROM EMP 
	WHERE to_char(HIREDATE,'DD')<'19';

14.	SELECT EMPNO 
	FROM EMP  
	WHERE ENAME='JAMES';

15.	SELECT SAL 
	FROM EMP 
	WHERE ENAME='KING';

16.	SELECT ENAME 
	FROM EMP 
	WHERE JOB='PRESIDENT';

17.	SELECT *
	FROM EMP 
	WHERE JOB='MANAGER';

18.	SELECT *
	FROM EMP 
	WHERE COMM IS NOT NULL;

19.	SELECT ENAME,EMPNO 
	FROM EMP 
	WHERE HIREDATE < '1-APR-81';

20.	SELECT ENAME ,SAL
	FROM EMP 
	WHERE ENAME IN('KING','BLAKE','FORD','SMITH')

21.	SELECT * 
	FROM EMP 
	WHERE SAL<3500;

22.	SELECT *
	FROM EMP 
	WHERE JOB='CLERK';

23.	SELECT ENAME,EMPNO,SAL,SAL*12 AS 'ANNUAL_SAL'
	FROM EMP;

24.	SELECT ENAME,SAL,SAL+SAL*20/100 AS 'INC_SAL'
	FROM EMP;

25.	SELECT DEPTNO,SUM(SAL)AS 'TOTAL_SAL'
	FROM EMP 
	GROUP BY DEPTNO;

26.	SELECT 
	MIN(SAL) AS 'MIN_SAL'
	FROM EMP
	WHERE DEPTNO='10';

27.	SELECT 
	AVG(SAL) AS 'AVG_SAL'
	FROM EMP 
	WHERE JOB='MANAGER'

28.	SELECT 
	SUM(SAL) AS 'TOTAL_SAL'
	FROM EMP 
	WHERE JOB='SALESMAN'

29.	SELECT DEPTNO,AVG(SAL)AS 'AVERAGE_SAL'
	FROM EMP 
	GROUP BY DEPTNO
	ORDER BY AVG(SAL);

30.	SELECT JOB,
	COUNT(JOB)
	FROM EMP 
	GROUP BY JOB

31.	SELECT MAX(SAL)
	FROM EMP 
	WHERE JOB='ANALYST'

32.	SELECT MAX(COMM)
	FROM EMP

33.	SELECT DEPTNO,COUNT(DEPTNO)
	FROM EMP

34.	SELECT ENAME 
	FROM EMP 
	WHERE ENAME LIKE '__L%'

35. 	SELECT ENAME, MIN(SAL) AS 'LOWEST SALARY'
	FROM EMP
	WHERE DEPTNO = 10;










